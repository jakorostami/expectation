# Three certified constructions for the sequential e-testing engine

Status: IMPLEMENTED (2026-09-11), plan v4 + reference audit; see Review. Bar applied: a foreign theorem must do real
work, the result must be an object the e-value world lacks, validity must be enforced by an
exactly computable certificate, and it must land at a specific place in this engine.

## Reference audit (user-requested; changes the novelty labels, not the constructions)

Searched the e-statistics literature for prior work on A, B, C. Findings, read this session:
- B is substantially anticipated. Pérez-Ortiz, Castro & Stoepker (2025; arXiv:2506.22588,
  v2 2026), "Anytime-Valid Tests for Sparse Anomalies": oracle test martingale
  E_t = prod_i {(1-eps) + eps LR_i(t)} (their Eq. 4), adaptive mixture over a discrete
  (eps, delta) grid (Sec. 2.2), detection moment t* = T* rho(beta) (Thm 2.1, 2.3, 2.6, 2.7).
  Also e-HC (OpenReview B4mu5A3wVN, 2025; authors not retrievable here) for p-value streams.
  Filtration contract: Wang, Dandapanthula & Ramdas (2025; arXiv:2502.08539) — local
  e-processes are global under independence across streams / a causal condition.
  => B is reframed as: PCS's construction in the engine with *pluggable per-stream
  e-processes* (their Gaussian LR grid is one instance; the library's mixture martingales
  absorb the delta-mixture), plus localisation diagnostics; their t* verified numerically.
- C is substantially anticipated. Martinez-Taboada & Ramdas (AISTATS 2025, PMLR 258;
  arXiv:2409.17505), "Sequential Kernelized Stein Discrepancy": payoff
  g_t(x) = sum_i h_p(X_i, x) / sum_i M_p(X_i) with per-point lower bounds
  M_p(x~) >= -inf_x h_p(x, x~), lambda in [0,1], aGRAPA/LBOW (Sec. 4, Thm 1, Thm 2); bounds
  derived per model for the IMQ kernel (Sec. 5); Zhou & Liu (2024) earlier with bounded kernels.
  => C is reframed as: SKSD in the engine, plus (i) a *generic closed-form* M_p from a Lipschitz
  constant of the score, valid for IMQ and for compactly supported Wendland kernels (their
  Sec. 5.1 structure with Gamma(u) = L u), and (ii) the compact-support variant under which
  E_p[h_p(X, y)] = 0 holds for every C^1 positive density with no moment condition
  (SKSD Thm 1 needs E[h_p(X,X)] < inf). Trade-off to document: compact support forgoes the
  tail-detection advantages of IMQ (Gorham & Mackey 2017).
- A sits inside a general theory but its computational layer is not found elsewhere.
  Larsson, Ramdas & Ruf (2025; arXiv:2504.02974), "Testing hypotheses generated by
  constraints": representation theorem for all e-variables of constraint-generated
  hypotheses (finitely many constraints, sub-psi, group symmetries; moments, quantiles,
  CVaR); Clerico (2024/25; arXiv:2412.21125) optimal e-variable sets for finitely many
  regular constraints; Ram, Larsson, Ruf & Ramdas (2026; arXiv:2606.24768) strong duality
  for GROW (information-projection pairs, GROW value = relative entropy for bounded
  e-variables); Larsson, Ramdas & Ruf, numeraire/RIPr (Ann. Statist. 2025). Fixed-sample
  Wasserstein robust testing: Gao, Xie, Xie & Xu (NeurIPS 2018). No e-process work found on
  Wasserstein-ball or Huber-contamination nulls, nor on LP-certified support functions.
  => A keeps its framing: the exact, certified, finite-outcome instantiation (LP support
  functions, Huber–Strassen recovery, Kantorovich-potential witness, EM RIPr, sequential
  plug-in, W1 distance CS) of that theory, credited to it.
- Set-aside item also anticipated: Koning (2026, JRSSB), "Anytime validity is free:
  inducing sequential tests" covers lifting fixed tests to sequential ones.

Honest novelty labels after audit: A = computational instantiation with certificates
(moderate); B = implementation of PCS 2025 + pluggable streams + diagnostics (modest);
C = implementation of SKSD 2025 + generic Lipschitz certificate + compact-support variant
(modest). All three remain capabilities this engine lacks.

Ground rules: new self-contained modules; log-space state; frozen Pydantic configs/results;
ABCs at extension points; NumPy/SciPy only; no dependence on audited-defective paths; the
library's `EProcess`/`EProcessUpdater` combiners do temporal betting; tests use exact checks
wherever the object is finite, Monte Carlo otherwise.

## Shared (small)
- [x] `expectation/modules/protocol.py` — `SkepticStrategy` ABC (`bet(x, announcement=None)
      -> float`, `reset()`), `ProtocolStepResult` (frozen), `TestingProtocol` orchestrator
      wiring a strategy to `EProcess` + `EProcessUpdater`, Ville, p-process, history, reset;
      validates emitted e-values (finite, >= 0) before any state change.
- [x] `tests/modules/test_protocol.py`.

## A — Upper-expectation nulls on finite outcome spaces, with certified log-optimal e-variables
Transfer: GTP upper expectations (Shafer & Vovk 2019) + Huber–Strassen least-favourable pairs
(1973, Ann. Statist.) + Kantorovich–Rubinstein duality / Wasserstein DRO + vertex reduction and
RIPr (Grünwald, de Heide & Koolen 2024 JRSSB; Larsson, Ramdas & Ruf 2024; Csiszár & Tusnády
1984). New object: e-variables valid for a whole uncertainty set, certified by an exactly
computed support function, with the set's own duality supplying Skeptic's optimal strategy.

Math (finite outcome space of size K, null set 𝒫, e-variable E >= 0):
- Validity iff sigma_𝒫(E) := sup_{P in 𝒫} <P, E> <= 1. Certify: E <- E / max(1, sigma_𝒫(E)).
- Contamination 𝒫 = {(1-eps) P0 + eps H}: sigma = (1-eps)<P0,E> + eps max E (closed form).
  Optimal vs belief q: E* = min(q/P0, c)/Z with (c, Z) from 1-D root finding so sigma = 1;
  verified against a generic convex programme (maximise <q, log E> s.t. sigma <= 1).
- Wasserstein 𝒫 = {Q : W1(Q, P0) <= rho} with cost matrix C: sigma via transport LP
  (`scipy.optimize.linprog`, HiGHS). Witness form: for 1-Lipschitz h (w.r.t. C),
  E = 1 + lam [ (h - <P0,h>) - rho ] has <Q,E> <= 1 on the ball since <Q,h> - <P0,h> <= rho.
  Optimal h = Kantorovich potential from the LP dual between q and P0; lam by 1-D log-optimal
  search on [0, 1/(rho + <P0,h> - min h)]. Growth possible iff W1(q, P0) > rho.
- Polytope 𝒫 = conv{P_v}: sigma = max_v <P_v, E>. Optimal E* = q/p*, p* = RIPr of q by
  mixture-weight EM; certified. CHSH: 16 local-deterministic vertices; singlet at Tsirelson
  angles gives S = 2 sqrt 2 and KL(q||p*) > 0; local vertices give |S| <= 2, KL = 0, E ≡ 1.
- Sequential: belief q_{t-1} = KT-smoothed empirical law (predictable) or fixed alternative;
  E_t certified each step; test supermartingale under any adapted choice P_t in 𝒫.
- Distance CS: run the Wasserstein Skeptic on a grid of rho; the rejected set is {rho < rho_t}
  (tax monotone in rho) -> anytime-valid lower confidence bound on W1(Reality, P0).

Files
- [x] `expectation/upperexp/__init__.py`, `config.py` (`UpperExpectationConfig`,
      `UpperExpectationStepResult`), `nulls.py` (`UpperExpectationNull` ABC:
      `support_function`, `certify`, `optimal_e_variable(q)`; `ContaminationNull`,
      `WassersteinNull`, `PolytopeNull`), `skeptic.py` (`UpperExpectationSkeptic`,
      `WassersteinDistanceSequence`), `bell.py` (CHSH vertices, singlet law, S value).
- [x] `tests/upperexp/test_nulls.py` — LP support function equals brute force on tiny spaces;
      certified E satisfies sigma <= 1 exactly; Huber–Strassen form recovered; KR duality:
      growth > 0 iff W1(q,P0) > rho; RIPr KL matches scipy; Tsirelson checks.
- [x] `tests/upperexp/test_skeptic.py` — Monte Carlo E[M_t] <= 1 and time-uniform rejection
      <= alpha under adversarial adapted P_t in 𝒫 (all three sets); power outside the set;
      distance-CS coverage; reset == fresh; invalid inputs rejected before state changes.

## B — Sparsity-adaptive global-null e-process for many independent streams
Transfer: Ingster's sparse-mixture likelihood ratio (1997) and the Donoho–Jin detection
boundary (2004, Ann. Statist.); the freezing/participation-ratio picture of Derrida's random
energy model as interpretation only. New object: mixture over sparsity of the library's
lambda-product merge applied to e-*processes* in time, giving an anytime-valid global test
that is powerful exactly where the arithmetic-mean merge is powerless.

Math: streams i = 1..K independent, each M_i(t) a test supermartingale under its null.
F_eps(t) = prod_i [(1-eps) + eps M_i(t)] is a nonnegative supermartingale (product of
independent nonnegative supermartingales); E_t = sum_j pi_j F_{eps_j}(t) is one too;
Ville applies. Posterior over eps and responsibilities r_i = eps M_i/((1-eps)+eps M_i)
localise evidence; participation ratio (sum r_i)^2 / sum r_i^2 is the diagnostic.
Contract: independence across streams (same as `ProductMerger`).

Files
- [x] `expectation/modules/sparsemixture.py` — `SparseMixtureConfig` (n_streams, eps grid or
      prior, significance_level), `SparseMixtureStepResult` (frozen: log_e, e, reject,
      posterior_eps, responsibilities, participation_ratio, step),
      `SparseMixtureGlobalNull.update(log_e_processes)`; log-sum-exp throughout.
- [x] `tests/modules/test_sparsemixture.py` — single-eps case equals `LambdaProductMerger`
      exactly; Monte Carlo E[E_t] <= 1 and time-uniform rejection <= alpha under the global
      null with independent Gaussian streams; the 100-of-300K witness where the arithmetic
      mean merge stays below 1 and the sparse mixture rejects; detection-boundary sweep at
      K = 1e5, single step, beta in {0.6, 0.8}, one r clearly below and one clearly above
      rho*(beta); planted streams get top responsibilities.

## C — Stein tickets against a Forecaster who can only announce a score
Transfer: Stein's identity / kernel Stein discrepancy (Gorham & Mackey 2015; Liu, Lee &
Jordan 2016; Chwialkowski, Strathmann & Gretton 2016) + compactly supported Wendland kernels
(1995). New object: a certified bounded zero-mean payoff for unnormalised targets, so the
library's combiners can bet on goodness of fit without a partition function.

Math: k_p(x,y) = s(x).s(y) k + s(x).grad_y k + s(y).grad_x k + sum_i d_{x_i} d_{y_i} k with
k(x,y) = phi(||x-y||/r), phi Wendland of order l = floor(d/2)+2. E_p[k_p(X,y)] = 0 for every C^1
positive density p (divergence theorem on the compact support). With ||s(x)-s(y)|| <= L||x-y||:
|k_p(x,y)| <= B(y) = (||s(y)||+Lr)||s(y)|| K0 + (2||s(y)||+Lr) K1 + K2,
K0 = 1, K1 = (l+2)(l/(l+1))^l / r, K2 = (l+1)(l+2) d / r^2. Witness g_t(x) = mean_{i<t}
k_p(x, X_i), bound B_t = mean_i B(X_i) (predictable). Emit E_t = 1 + g_t(X_t)/B_t in [0,2],
conditional mean exactly 1 under H0 (X_t | F_{t-1} ~ p); combiners bet 1 + lambda g_t/B_t.
E_q[g_t] -> KSD^2(q,p) >= 0. Null caveat: conditional law p; autocorrelated MCMC violates it.

Files
- [x] `expectation/stein/__init__.py`, `config.py` (`SteinConfig`, `SteinStepResult`),
      `kernels.py` (`SteinKernel` ABC, `WendlandSteinKernel`), `target.py` (`ScoreTarget`,
      `GaussianTarget`, `BoltzmannTarget`), `skeptic.py` (`SteinSkeptic`).
- [x] `tests/stein/test_kernels.py` — quadrature E_p[k_p(X,y)] = 0 (1-D Gaussian, 1-D
      double-well Boltzmann; 2-D Gaussian by Monte Carlo); |k_p| <= B(y) on fine grids;
      constants dominate grid maxima of |phi'|/r and |Laplacian|.
- [x] `tests/stein/test_skeptic.py` — Monte Carlo time-uniform type-I <= alpha through the
      protocol; power vs shifted and heavier-tailed q; witness predictability; reset.

## Cross-cutting
- [x] README bullets + references; AGENTS.md notes for protocol layer and three modules.
- [x] `black --check`, `isort --check-only`, full `pytest -m "not performance"` green.
- [x] Review section with verification evidence actually obtained.

## Considered and set aside (swappable)
- Doob-martingale lift of fixed-n tests, P0(reject | F_t)/alpha (Müller–Schäfer conditional
  rejection principle): exactly valid, clinically useful, but a recognition rather than a
  construction.
- Kalman-innovations state-space monitoring; CTW universal bettor; fixed-share switching;
  kernel two-sample betting; betting CS.

## Review (what was built and what the computer verified)

Files added: `expectation/modules/protocol.py`, `expectation/upperexp/{__init__,nulls,skeptic}.py`,
`expectation/stein/{__init__,kernels,skeptic}.py`; `SparseMixtureMerger` added to
`expectation/modules/merging.py` (an `EValueMerger` with posterior-mean gambling system);
tests in `tests/modules/test_protocol.py`, `tests/modules/test_merging.py`, `tests/upperexp/`,
`tests/stein/`; three executed notebooks in `examples/`. Existing files touched:
`merging.py` (additive), `epower.py` (one-token fix: `is_positive=bool(...)`, which removed a
pre-existing `np.bool` Pydantic deprecation warning), README, AGENTS.md.

Code-review rework (user feedback): sparse mixture moved from its own module into `merging.py`
as a merging function; Bell helpers folded into `CHSHLocalRealismNull(PolytopeNull)`; config
models folded into `skeptic.py` modules; abstract-property stacks replaced by plain attributes
and abstract methods; targets are `ScoreTarget` subclasses implementing `_score`; distance
sequence reuses `EProcess`/`EProcessUpdater`; growth rate reuses `EPowerCalculator`; module
docstrings cite sources and sections in the codebase's format.
Full suite: 457 passed, 2 pre-existing skips (`pytest -m "not performance"`, ~57 s);
`black --check` and `isort --check-only` clean repo-wide; mypy reports nothing in the new files.

Deviations from plan, all deliberate:
- Protocol orchestrator named `BettingProtocol` (not `TestingProtocol`: pytest collects `Test*`).
- `SkepticStrategy` has a two-phase `bet`/`observe` contract so a rejected e-value leaves
  strategy state untouched (avoids the RIPr-02 class of defect).
- No scalar ONS rule was added: the library's `EmpiricallyAdaptiveCombiner` already is the
  GRAPA-type bet on `g = E - 1`, so the Stein Skeptic emits `E_t = 1 + g_t` and reuses it.
- Sparse-mixture participation ratio uses positive per-stream log contributions (zero at
  capital one) rather than responsibilities (which equal eps for a null-looking stream).
- Wasserstein optimal e-variable skips the LP certificate: validity is exact by
  Kantorovich-Rubinstein plus a violation tax for the potential's LP tolerance.
- Polytope membership is read from the RIPr KL (< 1e-9 nats) instead of a separate LP.

Evidence, A (upperexp; 39 tests):
- Contamination support function equals the point-mass-adversary brute force to 1e-12;
  certified e satisfies `<Q, e> <= 1` for 200 random contaminants exactly.
- Optimal e-variable has the Huber-Strassen form `min(r/kappa, m)` (constant kappa on the
  unclipped set) and its growth equals a generic SLSQP GRO solution to 1e-6.
- Wasserstein: LP support function matches the two-point closed form; `W1` matches the 1-D
  CDF formula to 1e-9; the dual potential is 1-Lipschitz and attains `W1`; witness growth
  equals the generic GRO to 1e-4 (the witness family is numerically optimal on K=3);
  growth > 0 iff `W1(q, P0) > rho`.
- Polytope: RIPr KL equals direct SLSQP KL minimisation to 1e-7; certified `q/p*` satisfies
  every vertex; growth equals KL. CHSH: all 16 local vertices have |S| = 2, 50 random local
  mixtures have |S| <= 2, singlet at Tsirelson angles has |S| = 2 sqrt 2 and KL > 0.01,
  local mixtures have KL = 0 and `e ≡ 1`.
- Sequential: under an adversary choosing the least favourable set member after seeing the
  bet (all three sets), mean final capital <= 1 within 3 s.e. and time-uniform rejections
  within the binomial band at alpha = 0.1; power outside each set; per-round bid-ask
  announcements work; plug-in Skeptic detects the singlet without knowing it; quantum data
  grow at the KL rate (within 25% over 1500 rounds); Wasserstein distance sequence covers
  time-uniformly and its bound rises toward the true distance.

Evidence, B (sparsemixture; 11 tests):
- Single-eps output equals `LambdaProductMerger` to 1e-12; mixture equals the prior average
  of lambda-products to 1e-12 relative.
- Global null with 300 independent streams driven by `OneSidedNormalMixture` (known unit
  variance): mean capital <= 1 within 3 s.e., time-uniform rejections within band.
- 10 planted streams at e = 1000 among 20,000: arithmetic-mean merge < 20 (no rejection at
  alpha = 0.05), sparse mixture rejects, planted streams are the top-10, participation ratio ≈ 10.
- PCS detection moment: K = 2000, beta = 0.75, T* = 40 (t* = 10): cumulative rejection by
  t = 3 was <= 25%, by t = 40 >= 85% over 20 replications.

Evidence, C (stein; 24 tests):
- IMQ and Wendland radial derivatives match finite differences; `psi'(u)/u` finite at 0.
- Stein identity by quadrature: |E_p h_p(X, y)| < 1e-7 for N(0.3, 1.5) and for the double-well
  Boltzmann density (score not globally Lipschitz), three reference points each, both kernels;
  2-D Gaussian Monte Carlo mean within 4 s.e. of zero.
- Kernel constants dominate 200,001-point grid maxima for IMQ (d = 1, 2, 5) and Wendland
  (d = 1, 2, 3, 6); certified `M_p(y)` bounds `-min_x h_p(x, y)` on 60,001-120,001-point grids
  for Gaussian (both kernels) and double-well with local Lipschitz (Wendland).
- Our IMQ Gaussian constant `y^2 + y + 0.587` is below SKSD's hand-derived `y(1+y) + 3`.
- Through the protocol with the empirically adaptive combiner: time-uniform type-I within
  band for both kernels (100 reps x 60 rounds); power against a unit mean shift (both
  kernels) and against t(2) tails (IMQ); double-well Boltzmann target with local Lipschitz
  and Wendland kernel: exact null draws keep capital <= 1, a shifted sampler is rejected.

Honest novelty labels: A = computational, certified instantiation of the constraint-
hypothesis / GROW theory for three concrete sets (moderate); B = implementation of
Pérez-Ortiz, Castro & Stoepker (2025) with pluggable streams and diagnostics (modest);
C = implementation of Martinez-Taboada & Ramdas (2025) with generic Lipschitz certificates
and a compact-support variant (modest).

Example notebooks (rewritten in the repository's house style, all cells executed, zero
errors). **Simulation methodology:** nothing the method is supposed to discover is
hardcoded. Anomaly indicators are Bernoulli draws so counts are random; effect sizes come
from distributions chosen on domain grounds; ordinal outcomes come from an ordered-probit
latent mechanism; contaminated records are actually injected; correlated residuals are
built from a common factor; change points are drawn. Horizons are fixed from domain
reasoning and the outcome is reported whatever it is, with replications behind every
detection claim.
- `examples/imprecise-forecasters-upper-expectations.ipynb` — 12 sections: numeraire +
  certificate, Wasserstein ball (latent shift 0.45 -> W1 0.473; 12/12 detected at rho=0.20,
  0/12 at rho=0.60), distance lower bound, contamination with 3% corruption genuinely
  injected (0/10 false audits on spec, 10/10 on a degraded process, median 92 records),
  polytope against a regime blend redrawn every round plus two shocks - the mild one is
  outside the hull yet correctly *not* claimed, since its KL predicts ~3294 rounds against
  a 2000-round horizon - type-I on the set boundary (3/100), clinical non-inferiority
  (6/6, median 162 patients), supplier audit (10/10, median 197 units), LLM release gate
  (blocked at 343 responses, certified 0.30 judge points), bid-ask random walk (146
  events), CHSH (155 trials, 0.032 nats/trial vs 93 predicted).
- `examples/sparse-anomaly-global-null.ipynb` — 13 sections. The 50-world replication
  overturned the original framing: both merges detect 48/50, and the arithmetic mean is
  *earlier* in 25 worlds vs the sparse mixture's 13. The sparse mixture's real advantage
  is +33 nats of median evidence and the localisation that comes with it. Also: e-BH,
  a single-failed-stream case where the mean wins outright, the beta sweep against
  `detection_boundary`, type-I (0.010 / 0.015 over 200 sessions), and five replicated
  real-world monitors (neuroimaging K=20000 through clinical safety K=3000).
- `examples/stein-score-only-goodness-of-fit.ipynb` — 8 sections: IMQ vs N(0,1) (0/10
  false, 10/10 on a 0.4 shift, median 100 draws), standardised t(3), Wendland on the
  double well (exact sampler 0/6, tamed ULA 6/6 median 260), betting-strategy comparison,
  type-I (0.005 over 200 audits), two-factor residual validation (0/6 when the model
  holds; 6/6 median 424 for a weak common factor and 400 for understated volatility),
  Boltzmann trap at the wrong temperature, and a How-It-Works section stating the
  conditional-null and mixture-weight blind spots.
Notebooks use only library APIs plus data simulation; no new mathematics is defined in
them. The README's sparse-merge bullet was corrected twice to match what the replications
actually show (multiplicative evidence gain; comparable-to-slightly-later alarm times).

A third notebook, `examples/commercial-playbook.ipynb`, is a 22-section handbook (149
cells, 13 figures, executes in ~45 min). Part I foundations (capital-is-evidence with a
measured peeking comparison; choosing the lens; duration planning; the three settings that
decide validity). Part II adds **Defensive Forecasting** as a concept - Vovk, Takemura &
Shafer's fixed-point construction, noted as collapsing to the intermediate value theorem
in the binary case (Kakutani being needed in general) - in 13 lines, used as an online
recalibration layer: it cuts a deployed churn model's calibration error from 0.138 to
0.009 (a perfect model scores 0.007) while preserving ranking quality, with Skeptic's
capital never rising above zero. Part III works ten commercial settings, each with the
incumbent method **run and shown failing**, a sensitivity table and a runbook. Part IV
covers two-speed alerting, dependence and failure modes.

**A real library defect surfaced from writing it:** `SequentialTesting(test_type="variance")`
rejects a TRUE null in 200/200 runs of pure N(0,1) noise, one- and two-sided, while the
mean path on identical data sits at 0.005. This is SEQ-02 in the audit, now measured. Both
`commercial-playbook.ipynb` (§2.1) and `toolkit-tour.ipynb` now run a null check across
their lenses and label the variance path unusable; the insurance chapter keeps it
shadow-only and builds its conclusion on a pre-declared variance bet plus a distance bound.

Measured incumbent-method failures now carried in the text: SRE threshold alerting gives
817 pages or zero detections with nothing usable between; monthly SLA tests reset each
month give a compliant vendor a 55% false-alarm rate over two years; unadjusted
per-creative t-tests produce 67.3% false discoveries; an exact-contract supplier null
false-flags a compliant feed in 8/8 quarters on 3% bad scans; a fixed-baseline marketplace
test false-alarms in 20/20 harmless runs; a 0.005-band drift returns chi-square p = 8e-11.
Honest null results kept as findings: a 28-day media flight retires nothing, a 10%
marketplace shock is outside the null yet unreachable in 4,000 rounds, a 0.75-visibility
QRNG cannot be certified, and a Langevin step of 0.05 is detected only 2/5 times.

Two further notebooks were added for onboarding and commercial use:
- `examples/toolkit-tour.ipynb` — 19 sections covering every shipped tool in plain
  commercial language: the four `SequentialTesting` metric types, the result object and
  history table, the four staking rules (incl. `LOG_OPTIMAL`'s required setup call), all
  five mixture martingales, both calibrator directions, `EPowerCalculator` as a duration
  estimate, the Rust engine (50k tests x 80 steps at ~25M updates/sec), all six
  multiple-testing methods incl. the `adjusted_` family, all six mergers side by side,
  the adjusters, `KSampleSequentialTest`, `QuantileABTest` (p50 vs p95 on a tail
  regression), a worked custom `SkepticStrategy`, `upperexp`, `stein`, and a **section 17
  stating plainly what is not finished** (`confseq` labels vs behaviour, `parametric`,
  `conformal`, the `e_holm` naming, the un-ported Rust sparse merge), plus a cheat sheet.
- `examples/commercial-case-studies.ipynb` — three end-to-end decisions with runbooks:
  a streaming service's onboarding experiment, a payments company's fraud monitoring at
  20k merchants with 15 analysts, and a systematic desk's strategy-decay question. Three
  traps are measured rather than asserted: stopping on significance gave a lift estimate
  **2.7x the truth** (2.70x -> 1.07x with a 21-day floor and a post-novelty estimation
  window); the sparse merger on correlated merchants false-alarmed **28% of the time
  against a 5% budget** while the arithmetic mean stayed at 0-2%; and a raw evidence
  threshold produced **280 notifications of which 239 later evaporated** vs 3 from
  `adjusted_e_bh`. Case 3's headline is that one strategy's Sharpe decay needs ~24 years
  of daily P&L, while its execution quality degrades detectably in ~1,750 fills (~0.3
  trading days), and that understating the assumed variance turns a 5% error budget
  into 14%.
Four bugs were caught and fixed during their construction, each by checking the printed
numbers rather than trusting the cell ran: a quantile test reporting `sample_size = 0`,
an invented e-value stream that was not a fair game, a custom skeptic capped below its
stopping time, and a guardrail simulation that claimed nothing fired while showing
capital of 1e200.

Not done / follow-ups: Rust port of the sparse mixture for the 300K-stream engine; larger-K
Wasserstein support (K^2 LP variables); Wendland kernels for d > 3 use l = floor(d/2)+2 but
positive-definiteness was not separately tested. NB1 takes ~11 min to execute end to end
(polytope EM and Wasserstein LPs inside replication loops); trim the replication counts if
that becomes a problem. The two new notebooks run in ~40s and ~18s respectively.

---

# examples/commercial-handbook.ipynb (2026-09-13)

Status: DONE. 87 cells (39 code), 10 figures, executes clean in ~7.5 min, 1.58 MB.

## Goal
A production handbook: ten commercial settings, each teaching how to *use* a part of the
library. Selection rule: anchor each case on capabilities the existing 17 notebooks
under-use, and do not re-run domains already covered by `commercial-playbook` /
`commercial-case-studies` / `sparse-anomaly` / `upperexp` / `stein` / `ksample`.

## Per-section template (applied to all ten)
`.1` domain and what the mistake costs -> `.2` designing the study (hypothesis + the
null-class/filtration/clock/downstream-use contract table) -> `.3` assumptions stated
explicitly, with "if false" -> `.4` data-generating mechanism -> `.5` which capabilities and
why, with the rejected alternative -> `.6` build -> `.7` read the plots -> `.8` runbook.

## The ten
1. Marketplace unit economics - `confseq` + `boundaries` (radius rebuilt by hand; miscoverage measured).
2. Card-network compliance - `boundaries.bernoulli_confidence_interval`, one-sided proportion e-test, `ksample` attribution.
3. CDN p95 SLA - `quantiletest.QuantileABTest`, `orderstatistics`, inverted `double_stitching_bound`.
4. Clearing-house margin - `GammaExponentialMixture` through `log_superMG(s, v)`, after the variance path fails its null check.
5. Warranty reserving - `GammaPoissonMixture` + custom `SkepticStrategy`/`BettingProtocol`, exposure carried in the announcement.
6. LLM bake-off - `SymmetryETest` x3 null classes, `EPowerCalculator` for lambda and for budget sizing.
7. Model-risk governance - all five `PToECalibrator`s, `EToPCalibrator`, arithmetic-mean vs product merge.
8. Experimentation platform - four `EProcessUpdater` strategies incl. `LogOptimalCombiner`, `optimize_lambda`.
9. Credit-risk validation - `LikelihoodRatioEValue`, `UniversalEValue`, `stein`, `upperexp.ContaminationNull`.
10. Telecom NOC - `par_seqtest`, `modules.adjusters`, global merge, `conformal` CUSUM + adaptive threshold.
11. Closing map: tool-per-question table, capabilities left to their own notebooks, findings, five habits.

Coverage: 23 of 28 source modules imported directly. Excluded on purpose:
`parametric.ttest_universal` (incomplete - documented in 11.3), `ksample.bernoulli` and
`conformal.conformal` (reached through their public wrappers), `utils.helper_functions`
(Plotly; pointed at in 11.2).

## Review

Verified rather than asserted, in-notebook:
- `SequentialTesting(test_type="variance")` rejects a TRUE null 200/200 (mean control 0.000).
  Section 4 runs this check first and then routes around it via the `(s, v)` interface.
  `commercial-playbook.ipynb` section 4 still reports this path's output as a working
  detection - that section needs revisiting.
- `ConfidenceSequence` radius reproduced from `boundaries.gamma_exponential_mixture_bound`
  to 1e-12, confirming `boundary_type` is metadata on that path.
- Both `modules.adjusters` satisfy the admissibility integral numerically (1.000, 0.999).
- All five p-to-e calibrators satisfy E[f(U)] <= 1 over 500k uniforms.
- **New finding:** the sign and Wilcoxon symmetry e-values are anti-conservative on tied
  data with lambda < 0 - false-alarm rate 1.000 vs a 0.05 budget on identical vendors, where
  Fisher is unaffected. 28% of Likert pairs are ties. Dropping ties restores 0.010. This is a
  library-level caveat worth a docstring note in `hypothesistesting.SymmetryETest`.
- **New finding:** `SequentialTesting(TestType.QUANTILE)` never populates `result.sample_size`.

Non-rejections kept and used: a compliant chargeback portfolio that is still provably
non-homogeneous across acquirers; a warranty book below its reserving basis; an LLM
evaluation whose approved budget is ~9x short of what e-power says it needs; a carefree
list naming 1 cell where e-BH names 5. Each drives a real decision in its runbook.

Every claim about detection is replicated (50-300 runs) and reported as a detection rate
plus median stopping time, with operating-characteristic ladders rather than single seeds.

## Review addendum: capability-coverage pass (2026-09-13, after user review)

User review found the handbook was demonstrating a minority of the library. Audit confirmed:
52/154 public items used (34%), 3 dead imports, enums passed as string literals, and
self-made plotting where `utils.helper_functions` exists.

Result after the pass: **143/154 = 93%** (97% excluding `parametric`, which does not work),
**0 dead imports**, 133 cells / 63 code / 16 figures, executes clean in ~13 min.
The 5 remaining non-`parametric` items are internal math helpers (`log_beta`,
`log_incomplete_beta`, `find_s_upper_bound`), not user-facing capabilities.

Capabilities given a commercial home in this pass: full `boundaries` family (S4.8), all five
mergers + `create_merger`/`gambling_system` (S7.9), the four combiner objects +
`EPowerType` modes (S8.8), `WassersteinDistanceSequence`/`PolytopeNull`/`growth_rate`
(S9.10-9.11), `WendlandSteinKernel`/`CallableScoreTarget`/`BoltzmannTarget` (S9.12), the
`EValue` ABC as a VaR backtest (S9.13), `SparseMixtureMerger` + diagnostics (S10.9),
`ConformalEValue` + pseudomartingales + `EfficiencyAnalyzer` (S10.10), the typed
`par_seqtest` enums + adjuster factory (S10.11), `OrderStatisticInterface` via a histogram
implementation (S3.8), effect-size `ksample` + `DivergenceType` + `BernoulliRIPrCalculator`
(S2.8), `BoundaryConfig` (S2.9), the Plotly dashboards (S2.9), and a new Section 11
(QRNG certification) for `CHSHLocalRealismNull`. Map renumbered to Section 12.

Two further library defects found and documented (S12.3), both by pointing a tool at a
known answer:
- `EmpiricalBernsteinConfidenceSequence` returns `[68.7, 10.0]` for a true mean of 7.0 on
  `[0,10]` data - bounds rescaled by the support width on output. State is correct.
- `utils.plot_sequential_comparison_plotly` raises `TypeError` for every input:
  `helper_functions.py:877` indexes `colors["grid"]` after `colors` was rebound to a list.
  The other three plotters are unaffected.

Neither is fixed here (notebooks demonstrate, they do not patch). Both are candidates for a
follow-up alongside the `TestType.VARIANCE` defect.
